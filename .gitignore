# Clojure CLI dependency cache. Machine- and path-specific: it was checked
# in by the initial commit and removed alongside the CI change, which is
# what surfaced it. Recording the rule so it does not come back.
/.cpcache/
/target/
/node_modules/
/.DS_Store
